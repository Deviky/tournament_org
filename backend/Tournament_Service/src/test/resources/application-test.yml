spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Tournament_test
    username: postgres
    password: 915327
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true