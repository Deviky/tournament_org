eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

spring:
  application:
    name: AUTH-SERVICE

  config:
    import: optional:configserver:http://localhost:8888

  mail:
    host: smtp.ethereal.email     # сервер SMTP
    port: 587                   # порт
    username: tyshawn.upton83@ethereal.email
    password: BCUUYvQeDNuKwWBBBt
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

  datasource:
    url: jdbc:postgresql://localhost:5432/Tournament_org
    username: postgres
    password: 915327
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  redis:
    host: localhost
    port: 6379
    password: 915327
    database: 0


logging:
  level:
    org:
      springframework:
        web: DEBUG
    root: INFO

server:
  port: 8077
