spring:
  application:
    name: Notification_Service

  mail:
    host: smtp.ethereal.email     # сервер SMTP
    port: 587                   # порт
    username: deshaun52@ethereal.email
    password: ZJEVNwUwCc5k6m5zNd
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: notification-service
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"
        spring.json.value.default.type: com.deviky.Notification_Service.dto.Notification

  datasource:
    url: jdbc:postgresql://localhost:5432/Tournament_org
    username: postgres
    password: 915327
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8079

logging:
  level:
    org:
      springframework:
        web: DEBUG
    root: INFO